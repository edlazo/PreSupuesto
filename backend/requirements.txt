# FastAPI application
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0
python-dotenv>=1.0.1

# Supabase (PostgreSQL)
supabase>=2.10.0

# Hermes Agent API server client
httpx>=0.27.0

# Gemini fallback, used when the Hermes gateway is unreachable
google-genai>=2.0.0

# Budget PDF rendering
reportlab>=4.2.0

# MCP server that exposes the budget tools to Hermes Agent
# The 2.x low-level API (constructor handlers) is what mcp_server.py targets.
mcp>=2.0.0,<3.0.0
anyio>=4.6.0
