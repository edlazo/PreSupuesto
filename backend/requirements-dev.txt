# What the test suite needs on top of requirements.txt.
-r requirements.txt
pytest>=8.3
# Reads the text back out of the generated PDFs.
pypdfium2>=4.30
# Parses supabase/schema.sql and the migrations with PostgreSQL's own parser.
pglast>=7.0
